{
  "name": "proof-submit-sdk",
  "version": "1.0.0",
  "description": "Allow users to run proofs in the browser and submit on-chain",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "proto": "protoc --plugin=node_modules/ts-proto/protoc-gen-ts_proto proto/miden_prover.proto proto/context.proto --ts_proto_out=./src/proto-ts -Iproto",
    "wasm-build": "cd miden-wasm && wasm-pack build --target web --out-name miden-wasm --out-dir pkg && cd ..",
    "build": "npm run wasm-build && npm run proto && tsc"
  },
  "keywords": [
    "zkp",
    "miden"
  ],
  "author": "starkoracles",
  "license": "MIT",
  "dependencies": {
    "miden-wasm": "file:./miden-wasm/pkg",
    "protobufjs": "^7.2.3",
    "ts-proto": "^1.146.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^18.15.11",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}